---
const { title } = Astro.props;
---

<article
  class={`${Astro.props.class} px-2 py-4 relative rounded-xl overflow-hidden group`}>
  <slot name="image" />

  <div class="select-none flex flex-col gap-1 p-6 text-lg justify-end h-full">
    <h2 class="text-2xl md:text-4xl font-playfair hover:z-50">{title}</h2>
    <slot name="content" />
  </div>
</article>
